{
  "name": "quick-copy-panel",
  "version": "1.0.0",
  "private": true,
  "description": "A local-first floating browser extension for one-click text copying.",
  "scripts": {
    "check": "node scripts/validate-extension.js && node --check src/model.js && node --check src/content.js && node --check src/background.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=18"
  }
}
